#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int a, b,c;
	scanf_s("%d %d",&a, &b);
	c = a % b;
	if (c == 0)
	{
		printf("材计琌材计计");
	}
	else
	{
		printf("材计ぃ琌材计计");
	}

	system("pause");
	return 0;
}